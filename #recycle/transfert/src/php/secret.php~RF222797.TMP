<?php

function getDailyKey(): string {
    $secret = getenv('TRANSFERT_SECRET_SEED');
    
	if ($secret === false || $secret === '') {
        // Valeur temporaire si la constante n'est pas définie dans config.php
        $secret = '24Bo!CHG20Y{';
    }
	
    $day = date('Ymd'); // Format : 20250528
    return hash('sha256', hash('sha256', $secret . $day));
}
